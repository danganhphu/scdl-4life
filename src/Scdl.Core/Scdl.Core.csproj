<Project Sdk="Microsoft.NET.Sdk">

  <!-- RootNamespace and AssemblyName are left at their defaults, which already
       match the project file name. -->

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" />
    <PackageReference Include="Microsoft.Extensions.Http" />
    <PackageReference Include="Microsoft.Extensions.Http.Resilience" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.Options" />
    <PackageReference Include="z440.atl.core" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Scdl.Core.Tests" />

    <!-- Moq generates proxies into this assembly at run time, so it needs the
         same access in order to mock an internal interface such as IMediaMuxer. -->
    <InternalsVisibleTo Include="DynamicProxyGenAssembly2" />
  </ItemGroup>

</Project>
