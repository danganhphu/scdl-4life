# TUnit runs on Microsoft.Testing.Platform. The .NET 10 SDK no longer routes
# `dotnet test` through VSTest, so the runner has to be named explicitly.
[dotnet.test.runner]
name = "Microsoft.Testing.Platform"
